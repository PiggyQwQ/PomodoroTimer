# 标题字体（可选）

应用标题使用「汉仪心海行楷W」书法字体。

该字体为**汉仪商业字体**,出于授权原因**不随仓库分发**(其授权声明禁止向第三方平台公开发布下载)。

## 如何启用标题字体

任选其一:

1. **安装到系统** —— Windows 下双击 `汉仪心海行楷W.ttf` 安装,程序启动时会自动识别。
2. **放入本目录** —— 将 `汉仪心海行楷W.ttf` 复制到当前 `fonts/` 文件夹下。

两者都没有时,标题会自动回退为系统默认字体,程序功能不受影响。

字体加载逻辑见 `main.py` 中的 `load_title_font()`。
