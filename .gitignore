# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Pomodoro runtime data (stored in user home, but ignore if copied here)
.pomodoro_data.json

# Commercial title font — not redistributable, must not be committed
fonts/*.ttf
fonts/*.otf
fonts/*.ttc

# Local dev tooling — Claude Code skills/settings, not part of the app
.claude/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
